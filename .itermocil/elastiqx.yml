windows:
  - name: elastiqx
    root: ~/Project/my-projects/ddux-poc
    layout: double-main-vertical
    panes:
      - mongod
      - git status
      - IS_PRODUCTION=true SERVER_NAME=localhost npm start
