windows:
  - name: qr
    root: ~/Project/trampo/qultureapp
    layout: double-main-vertical
    panes:
      - rails s
      - rails c
      - redis-server
      - bundle exec sidekiq -C config/sidekiq.yml
      - git status
